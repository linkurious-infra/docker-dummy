node ("docker-agent-jnlp") {
  stage('Hello')
  {
    echo 'Hello World'
  }

  stage('Prepare') {

    // Get repository
    checkout scm
  }
  stage('Sleep') {

    // Get repository
    sleep(60)
  }
}
